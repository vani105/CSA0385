#include<stdio.h>
int main()
{
    int sum=0;
    int arr[5]={1,5,15,24,30};
    int length = sizeof(arr) / sizeof(arr[0]);
    for(int i=0;i<5;i++)
    {
        sum+=arr[i];
    }
    printf("sum is %d",sum);
    return 0;
}
